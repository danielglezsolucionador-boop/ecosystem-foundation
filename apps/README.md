# Applications

Contiene las aplicaciones ejecutables del repositorio `ecosystem-foundation`.

Estado inicial:

- `api/`: backend base del ecosistema.
- `web/`: futuro Control Center web.

Reglas:

- no incluir secrets;
- no tocar FORJA;
- no tocar CEREBRO;
- no hacer deploy desde esta carpeta en la fase actual.

